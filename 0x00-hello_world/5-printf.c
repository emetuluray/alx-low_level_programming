#include <stdio.h>

/**
 * main: Entry point
 * printf: it prints with proer grammer
 * return: returns 0
 */
int main(void);
{
	printf("with proper grammer, but the outcome is a piece of art,");
	return (0);
}
